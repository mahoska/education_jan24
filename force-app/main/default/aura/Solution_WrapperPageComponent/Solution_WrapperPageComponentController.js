({
    changeSliderTimeLine : function(component, event, helper) {
        helper.setTimeLine(component);
	},

    changeToggle : function(component, event, helper) {
        helper.setTimeInterval(component);
    },

})